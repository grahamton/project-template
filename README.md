# Project Template

This repository holds reusable templates. Start minimal and scaffold features per project.

## Web App Template
- Install deps:
  - `python -m pip install -r templates/webapp/requirements.txt`
- Run locally:
  - `uvicorn --app-dir templates webapp.main:app --reload --port 8000`
  - Open `http://localhost:8000`

